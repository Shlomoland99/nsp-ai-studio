# API backend

FastAPI service exposing health and job submission endpoints. Authentication, persistence, and Stripe webhooks are configured through environment variables and are intentionally not populated with secrets.