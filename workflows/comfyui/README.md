# ComfyUI workflows

These are starter API-format templates. Install the matching checkpoints and custom nodes locally, then export a tested API workflow from ComfyUI. Model weights are intentionally excluded.