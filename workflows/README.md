# Workflows

Named pipelines are declared in config/pipelines.yaml. ComfyUI workflow JSON exports may be stored under workflows/comfyui/ and must not contain secrets. Keep workflow assets small and provider-specific payload mapping inside the ComfyUI adapter.
